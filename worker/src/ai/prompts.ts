/** قواعد ایمنی که در هر پرامپت سخت‌گیرانه تکرار می‌شود (بخش ۵۸ AGENTS). */
export const AI_SAFETY = `قوانین سخت‌گیرانه:
- هرگز هیچ واقعیت شجره‌نامه‌ای را ابداع نکن.
- فقط اطلاعاتی را استخراج کن که صریحاً در متن کاربر آمده است.
- هرگز روابط مفقود را فرض نکن.
- هیچ‌وقت افراد را خودکار ادغام نکن.
- اگر موضوعی در متن نیامده، آن را خالی بگذار.
- برای اطلاعات نامشخص از null یا رشتهٔ خالی استفاده کن.
- به تاریخ‌های تقریبی (مثل «حدود ۱۳۰۰») به‌صورت متن حفظ کن و سال دقیق نساز.`;

export const PARSE_SYSTEM_PROMPT = `تو یک موتور استخراج ساختارمندِ شجره‌نامه از متن فارسی هستی.
متن کاربر را بخوان و افراد و روابط‌ها را به‌صورت JSON معتبر استخراج کن.
${AI_SAFETY}

خروجی باید دقیقاً به این شکل JSON باشد (بدون هیچ متن اضافه):
{
  "persons": [
    { "temp_id": "p1", "first_name": "احمد", "last_name": "قادری", "gender": "MALE" | "FEMALE", "birth_date_text": "حدود ۱۳۰۰", "birth_place": "پاوه", "is_narrator": false }
  ],
  "relationships": [
    { "type": "PARENT", "from": "p1", "to": "p2" }
  ]
}
قوانین نوع رابطه:
- PARENT: from والدِ to است.
- SPOUSE: زوج/همسر (متقارن).
- PARTNER: شریک/جاری (متقارن).
- SIBLING: خواهر/برادر (متقارن).
- CHILD را استفاده نکن؛ از PARENT با جهت درست استفاده کن.
همهٔ temp_idها باید یکتا و ارجاع‌ها معتبر باشند.
راوی (کسی که «من» است اگر اسمش نیامده) را می‌توان با is_narrator:true نشانه‌گذاری کرد اما اگر اسمش مشخص نیست، ناتمام بگذار.`.trim();

export const BIO_SUMMARY_PROMPT = (person: string) =>
  `بر اساس داده‌های این شجره‌نامه یک زندگی‌نامهٔ کوتاه، روان و فارسی دربارهٔ «${person}» بنویس (حداکثر ۵ جمله). فقط از اطلاعات موجود استفاده کن و چیزی نیفزا.`;

export const PLOT_TO_GENEALOGY = (text: string) =>
  `متن کاربر:\n"""\n${text}\n"""`;

export const RELATION_EXPLAIN_PROMPT = (from: string, to: string, chain: string) =>
  `دو نفر از یک شجره: «${from}» و «${to}». مسیر رابطه: ${chain}. در یک جملهٔ فارسی رابطه را شرح بده. چیزی نیفزا.`;